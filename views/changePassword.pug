extends layouts/main

block content
    .form-container
        form(action=`/users${routes.changePassword}`, method="post")
            input(type="password", placeholder="Current Password", name="password", require=true)
            input(type="password", placeholder="New Password", name="newPassword", require=true)
            input(type="password", placeholder="Verify New Password", name="newPassword2", require=true)
            input(type="submit", value="Change Password")
            